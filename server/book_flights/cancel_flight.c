#include <stdio.h>
#include <string.h>
#include "book_flights.h"

void cancel_flight(const char *flight_id, const char *passenger_details) {
    // Placeholder flight cancellation logic
    printf("Booking for flight %s and passenger %s cancelled\n", flight_id, passenger_details);
}
