#include <stdio.h>
#include <string.h>
#include "book_flights.h"

void book_flight(const char *flight_id, const char *passenger_details) {
    // Placeholder flight booking logic
    printf("Flight %s booked for passenger: %s\n", flight_id, passenger_details);
}
